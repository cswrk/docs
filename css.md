### Selector NOT
```css
.setPrev:not(.active) { .. }
.setNext:not(.active) { .. }
```

=== Show different parts of SVG
```css
.setPrev { background-position: 000% 000%; }
.setNext { background-position: 100% 000%; }
```
